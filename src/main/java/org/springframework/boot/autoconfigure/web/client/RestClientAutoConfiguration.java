package org.springframework.boot.autoconfigure.web.client;

/**
 * Stub class for Spring Boot 3.x compatibility — some auto-configuration classes
 * reference this class via @AutoConfiguration(before/after). In Spring Boot 4.x this
 * class was removed, so we provide a minimal stub to prevent TypeNotPresentException.
 */
public class RestClientAutoConfiguration {
}
